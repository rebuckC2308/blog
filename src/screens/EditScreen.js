import React, { useContext } from "react";
import { View, Text, StyleSheet } from "react-native";

const EditScreen = () => {
  return <Text>Edit Blogpost</Text>;
};

const style = StyleSheet.create({});

export default EditScreen;
